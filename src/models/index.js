const User = require('./user.model');
const Common = require('./common.model');
const SuperAdminUser = require('./super-admin-user.modal');

module.exports = {
    User,
    Common,
    SuperAdminUser
}